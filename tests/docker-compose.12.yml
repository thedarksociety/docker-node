version: '3'

services:
  
  node-alpine:
    build:
      context: ../image/12/alpine
      dockerfile: Dockerfile
    container_name: dscr_node-alpine

  node-buster:
    build:
      context: ../image/12/buster
      dockerfile: Dockerfile
    container_name: dscr_node-buster